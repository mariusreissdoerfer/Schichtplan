# Schichtplan

Simple round-robin shift schedule generator.

## Usage

Run the script to print a sample weekly schedule:

```bash
python schichtplan.py
```

## Tests

Execute tests with:

```bash
python -m unittest
```
